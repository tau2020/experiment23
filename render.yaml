services:
                                - type: web
                                    name: unified-app
                                    env: node
                                    region: oregon
                                    branch: develop
                                    buildCommand: 'echo 'No build step required''
                                    startCommand: 'node index.js'
                                    plan: starter
                                    autoDeploy: true
                                    envVars:
                                    - key: NODE_ENV
                                        value: production