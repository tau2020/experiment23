services:
                                - type: web
                                    name: unified-app
                                    env: node
                                    region: oregon
                                    branch: develop
                                    buildCommand: 'npm run build'
                                    startCommand: 'npm start'
                                    plan: starter
                                    autoDeploy: true
                                    envVars:
                                    - key: NODE_ENV
                                        value: production